/** @type {import('tailwindcss').Config} */
module.exports = {
  purge: [],
  theme: {
    extend: {},
  },
  plugins: [],
    CorePlugins: {
      // On a une option pour désactiver le style défini dans Tailwind. False = style inactif, true = style actif.
      // preflight: false
    }
}
